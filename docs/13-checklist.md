# 13. 実装チェックリスト

書き終わったらこれで自己検証し、**結果を報告する**。
「全部 ✅」ではなく、確認できなかった項目は「未確認」と書く。

---

## 色・面

- [ ] `grep -rnE '^\s*(-webkit-)?backdrop-filter\s*:' src/` が **0 件**
      （コメント中の「使わない」は当たらないよう、宣言だけを引く）
- [ ] `grep -rniE "#[0-9a-f]{3,8}\b" src/styles/` の結果が次だけ:
      生成物 `m3-scheme.css` / 写真・動画の上に置くもの（黒帯・白文字）/
      `mask` と `mask-image` の `#000`（色ではなく不透明度としてしか使われない）
- [ ] 選択状態が `secondary-container`、破壊的操作が error 系、主要操作が primary で統一
- [ ] カードは `surface-mid` で影なし。影があるのは浮いているものだけ
- [ ] `on-` 色が対で使われている（`primary-container` の上は `on-primary-container`）
- [ ] ダークとライトの両方で見た。切替でチラつかない

## 間隔・折り返し

- [ ] `grep -n "margin" src/styles/*.css` を目視し、`gap` を持つ列の子に `margin` が無い
      （相殺目的の負の margin は可。コメントで理由を書く）
- [ ] 1画面の間隔が2種類以内（話題の切れ目 / 同じ話題の中）
- [ ] 札（ボタン・チップ・タブ・区画）が `white-space: nowrap`。幅が足りないとき
      2行に折れるのではなく末尾が省かれる（**端末幅 360px で確認**）
- [ ] 伸び代（`flex: 1`）を同じ行に2つ置いていない
- [ ] grid の列が `minmax(0, 1fr)`（素の `1fr` になっていない）
- [ ] スクロールする flex 列の直接の子に `flex-shrink: 0` が効いている

## 形・動き

- [ ] ボタン・アイコンボタン・FAB が押下で**形**を変える（`scale` / `opacity` ではない）
- [ ] `:active` と `.is-pressed` の**両方**にスタイルが当たっている
- [ ] `transform` の transition が `--d-spatial*`、色が `--d-effects*`、押し込みが `--d-tap`
- [ ] `grep -n "linear" src/styles/*.css` の結果が、無限回転と
      `linear-gradient` と `linear()` だけ
- [ ] `width` / `height` / `top` / `margin` を transition していない
- [ ] Reduce Motion で 150ms のフェードに落ちる（DevTools の Rendering で確認）
- [ ] 指で掴める要素（シート・スナックバー・スワイプする行）に CSS の
      `@keyframes` を足していない

## 触感

- [ ] 引き下ろし・左スワイプ・ピンチが **1:1** で追従し、離した速度を引き継ぐ
- [ ] タップ要素が 48px 以上（当たり判定込み）。`::after` を使った要素の親に
      `position: relative` があり、`overflow: hidden` の中でクリップされていない
- [ ] `touch-action` を宣言してある
- [ ] Android の戻るで 1枚ずつ閉じる

## 状態

- [ ] 読み込み中 / 空 / エラー / 中身あり の**4つ全部**が定義されている
- [ ] 読み込みが骨（全画面スピナーが無い）。200ms 未満では出さない
- [ ] 空状態に「次にやる操作」のボタンが1つある
- [ ] エラーに「何が起きたか」「どうすれば直るか」「再試行ボタン」がある
- [ ] 更新系が楽観的更新 + スナックバーの「元に戻す」。
      確認ダイアログは本当に戻せないものだけ

## 重ねるもの（メニュー・シート・ダイアログ）

- [ ] 出し入れを `popover` 属性か `<details>` に任せている（自前の外側クリック検出が無い）
- [ ] 携帯でメニュー（`.menu`）を出していない。ボトムシートになっている
- [ ] ツールチップが `pointer: coarse` で出ない
- [ ] ダイアログを出しているのは**本当に戻せない操作だけ**
- [ ] `z-index` を `tokens.css` の `--z-*` から取っている（各所で発明していない）

## 画面幅（PC・タブレットにも出すなら）

- [ ] `.app--adaptive` を付け、`.navbar` / `.rail` / `.drawer--standard` を
      3つとも DOM に置いている（JS で出し分けていない）
- [ ] 左の占有量が `--inset-start` から計算されている
- [ ] 840px 以上で本文の1行が 90 文字を超えていない
- [ ] 一覧+詳細の2枚にしたとき、**詳細が空のときの状態**を作った
- [ ] 幅を狭めても入力中の値・スクロール位置が飛ばない
- [ ] hover でしか出ない操作が無い（携帯では永久に見つからない）
- [ ] 携帯で `.table` を使っていない（`.rowlist` に畳んである）

## 品質フロア

- [ ] 本文のコントラスト 4.5:1 以上
- [ ] 操作要素に読み上げラベル（`aria-label`）。選択状態が
      `aria-pressed` / `aria-selected` / `aria-current` で表現されている
- [ ] 入力欄の `font-size` が 16px 以上（iOS が拡大しない）
- [ ] キーボード表示時に入力欄が隠れない（`interactive-widget=resizes-content`）
- [ ] Safe Area（`--safe-top` / `--safe-bottom`）を使っている
- [ ] OS の文字サイズを最大にしても崩れない
- [ ] 横向き・720px 超の幅で間延びしない

## 速さ

- [ ] タップから視覚的な反応まで 100ms 以内
- [ ] `will-change` を常設していない（掴んでいる間だけ）
- [ ] 一覧が仮想化されている / サムネと原寸が分かれている
- [ ] フォントを同梱していない（等幅を除く）
- [ ] 配色を実行時に計算していない（`material-color-utilities` が
      `dependencies` ではなく `devDependencies` にある）

---

## 目で見る

- [ ] `demo/index.html`（見本帳）に新しい部品を足した
- [ ] 実機かエミュレータで見た。**紙の上の計算だけで終わっていない**

見ていないなら「未確認」と正直に書く。
